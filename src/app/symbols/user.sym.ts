export enum USER {
    SET_CREDENTIALS = '@user/set-credentials',
    REMOVE_CREDENTIALS = '@user/remove-credentials',
    REGISTER = '@user/register',
    AUTHENTICATE = '@user/authenticate',
    REVOKE = '@user/revoke',
    SET_CREDENTIALS_CORRELATION = '@user-set-credentials',
    SAVE_CREDENTIALS_CORRELATION = '@user-save-credentials',
    REMOVE_CREDENTIALS_CORRELATION = '@user-remove-credentials',
}
