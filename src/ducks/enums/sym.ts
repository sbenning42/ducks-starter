export enum SYMD {
    DUCKS = 'ducks',
    ASYNC = 'async',
    REQUEST = 'request',
    RETRY = 'retry',
    CANCEL = 'cancel',
    RESOLVED = 'resolved',
    ERRORED = 'errored',
    CANCELED = 'canceled',
    ASYNC_CORRELATION = '@async',
}